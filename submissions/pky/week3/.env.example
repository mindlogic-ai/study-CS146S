# API key for authenticating MCP clients (HTTP transport only)
# Generate any secure random string, e.g.: python -c "import secrets; print(secrets.token_urlsafe(32))"
HN_MCP_API_KEY=your-secret-api-key-here
